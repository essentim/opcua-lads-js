/**
 * The interface between the library's user-facing code and the low-level OPCUA server. It is a facade/factory to simplify
 * the creation of LADS devices.
 */
class LadsDeviceFactory {
    constructor() {
    }
}